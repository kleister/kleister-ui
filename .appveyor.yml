version: '{build}'
image: 'Visual Studio 2017'

clone_folder: 'c:\gopath\src\github.com\kleister\kleister-ui'

environment:
  GOPATH: c:\gopath
  DOCKER_USERNAME:
    secure: 'hiAQ2emetxm9/e0jpCnVjw=='
  DOCKER_PASSWORD:
    secure: 'K63dhBlHES6R/tOVKDhP/vpv0sRNeM2JIWlsfHUrE8Y='

install:
  - ps: |
      choco install make
  - ps: |
      node -v
      docker version
      go version
  - ps: |
      $env:Path = "c:\gopath\bin;$env:Path"

build_script:
  - cmd: |
      yarn install --frozen-lockfile
  - cmd: |
      yarn build
  - cmd: |
      make retool
  - cmd: |
      make sync
  - cmd: |
      make generate
  - cmd: |
      make build
  - cmd: |
      docker build -f docker/Dockerfile.windows.amd64 -t kleister/kleister-ui:windows-amd64 .

test_script:
  - cmd: |
      docker run --rm kleister/kleister-ui:windows-amd64 --help

deploy_script:
  - ps: |
      if ( $env:APPVEYOR_PULL_REQUEST_NUMBER ) {
        Write-Host Nothing to deploy.
      } else {
        echo $env:DOCKER_PASSWORD | docker login --username $env:DOCKER_USERNAME --password-stdin

        if ( $env:APPVEYOR_REPO_TAG -eq 'true' ) {
          $major,$minor,$patch = $env:APPVEYOR_REPO_TAG_NAME.substring(1).split('.')

          docker push kleister/kleister-ui:windows-amd64

          docker tag kleister/kleister-ui:windows-amd64 kleister/kleister-ui:$major.$minor.$patch-windows-amd64
          docker push kleister/kleister-ui:$major.$minor.$patch-windows-amd64

          docker tag kleister/kleister-ui:windows-amd64 kleister/kleister-ui:$major.$minor-windows-amd64
          docker push kleister/kleister-ui:$major.$minor-windows-amd64

          docker tag kleister/kleister-ui:windows-amd64 kleister/kleister-ui:$major-windows-amd64
          docker push kleister/kleister-ui:$major-windows-amd64
        } else {
          if ( $env:APPVEYOR_REPO_BRANCH -eq 'master' ) {
            docker push kleister/kleister-ui:windows-amd64
          } else {
            Write-Host Ignoring feature branch.
          }
        }
      }
